1. props helps us accomplish the reusabiliy of a component.

2. How to pass props:
    <Mycomponent title="thisisit"/>

3. How to recieve props in a component?
    function Navbar (props) {
        return (
            <header>
                ...
            </header>
        )
    }

4. props is an Object data type!

5. Instead of writing every component's props each time while passing, we can fetch the data from somewhere using a function using map method and create an array of that data.
6. The array will containing all the data and we use the array to pass the props. 